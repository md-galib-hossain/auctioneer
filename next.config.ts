import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['example.com','images.pexels.com','encrypted-tbn0.gstatic.com'],
  },
};

export default nextConfig;
